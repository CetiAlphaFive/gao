---
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.path = "man/figures/README-",
  out.width = "100%"
)
```

# A Complete Library of GAO Reports and Metadata <a href="https://github.com/CetiAlphaFive/gao"><img src="man/figures/logo.png" align="right" height="139" alt="gao hex sticker" /></a>

<!-- badges: start -->
[![R-CMD-check](https://github.com/CetiAlphaFive/gao/actions/workflows/R-CMD-check.yaml/badge.svg)](https://github.com/CetiAlphaFive/gao/actions/workflows/R-CMD-check.yaml)
[![CRAN status](https://www.r-pkg.org/badges/version/gao)](https://CRAN.R-project.org/package=gao)
[![Lifecycle: experimental](https://img.shields.io/badge/lifecycle-experimental-orange.svg)](https://lifecycle.r-lib.org/articles/stages.html#experimental)
<!-- badges: end -->

The U.S. Government Accountability Office (GAO) is the audit and
investigative arm of Congress. Since 1921 it has published tens of
thousands of reports on how the federal government spends money and how
well its programs work.

`gao` puts that entire library at your fingertips in R. It ships with a
built-in dataset covering every GAO report --- metadata, topics,
agencies, congressional requesters, and more --- updated daily, and it
can batch-download the full reports as PDF or HTML.

## Installation

```r
# From CRAN (when available)
install.packages("gao")

# Development version from GitHub
# install.packages("pak")
pak::pak("CetiAlphaFive/gao")
```

## Quick start

The dataset is bundled with the package, so browsing it works
immediately --- no internet connection or extra software needed:

```{r, message = FALSE}
library(gao)

reports <- gao_links()
nrow(reports)

# GAO reports on health care from fiscal year 2024
subset(reports, pub_fiscal_year == 2024 & topic_health_care == 1,
       select = c(report_id, published, title)) |>
  head(3)
```

Each row is one GAO product, with `r ncol(reports)` columns of metadata:
title, dates, summary, page count, topics, agencies reviewed,
recommendation counts, who in Congress requested the report, and
ready-made indicator and covariate columns for analysis (for example,
`topic_health_care`, `pub_fiscal_year`, `requester_party`).

To download the actual reports, use `auto_download()`:

```{r, eval = FALSE}
# Interactive --- prompts for format and year range
auto_download()

# Or specify everything up front
auto_download(format = "pdf", year = 2020:2024, confirm = FALSE)
```

This filters the dataset by fiscal year and saves the files into
`gao_reports/pdf/` and `gao_reports/html/` in your working directory.

The package has three main functions:

- **`auto_download()`** --- download reports as PDF, HTML, or both (or export the metadata as CSV)
- **`gao_links()`** --- access the full metadata dataset
- **`extract_text()`** --- pull plain text out of downloaded PDFs, ready for text analysis

## Downloading reports from gao.gov

Everything above works out of the box. Downloading report files directly
from gao.gov is the one feature that needs a helper program:
[curl-impersonate](https://github.com/lexiforest/curl-impersonate), a
drop-in variant of curl that gao.gov's servers accept.

```bash
# Arch Linux
sudo pacman -S curl-impersonate

# macOS
brew install lexiforest/curl-impersonate/curl-impersonate
```

Any function that needs it will tell you and print these instructions if
it is missing.

## Keeping the data current

New GAO reports are added to the dataset every day. To get the latest
version without waiting for a new package release:

```{r, eval = FALSE}
gao_update_data()
```

This downloads the freshest dataset (a few MB) and caches it locally;
`gao_links()` uses it automatically from then on.

## Learn more

- [The GAO report dataset](https://cetialphafive.github.io/gao/articles/gao-dataset.html) --- every column explained, with worked examples
- [Downloading reports and extracting text](https://cetialphafive.github.io/gao/articles/downloading-reports.html) --- from bulk download to text analysis
- [Function reference](https://cetialphafive.github.io/gao/reference/index.html)

## Disclaimer

This package is not affiliated with, endorsed by, or in any way
officially connected to the U.S. Government Accountability Office. All
data is obtained from public web pages at
[gao.gov](https://www.gao.gov).
