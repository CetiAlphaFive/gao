% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_text.R
\name{extract_text}
\alias{extract_text}
\title{Extract Text from GAO PDF Reports}
\usage{
extract_text(pdf_dir, pattern = "\\\\.pdf$", verbose = TRUE)
}
\arguments{
\item{pdf_dir}{Character. Path to a directory containing PDF files.}

\item{pattern}{Character. Regex pattern to filter filenames
(default: \code{"\\\\.pdf$"}).}

\item{verbose}{Logical. Show progress messages (default: \code{TRUE}).}
}
\value{
A data.frame with columns:
\describe{
\item{file}{Character. The PDF filename (without directory path).}
\item{text}{Character. The extracted text, with pages collapsed into a
single string separated by newlines.}
\item{pages}{Integer. The number of pages in the PDF.}
}
}
\description{
Reads PDF files from a local directory and extracts their text content using
\code{\link[pdftools:pdftools]{pdftools::pdf_text()}}. Designed to work with PDFs downloaded via
\code{\link[=auto_download]{auto_download()}}.
}
\details{
Requires the \pkg{pdftools} package. Install it with
\code{install.packages("pdftools")} if not already available.

Files that fail to parse (e.g., scanned/image-only PDFs) are included in
the result with \code{NA} text and \code{NA} pages along with a warning.
}
\examples{
\dontrun{
# After downloading PDFs
auto_download(format = "pdf", year = 2024, download_dir = "gao_reports")
texts <- extract_text("gao_reports/pdf")
head(texts$file)
nchar(texts$text[1])
}
}
