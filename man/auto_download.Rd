% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_download.R
\name{auto_download}
\alias{auto_download}
\title{Download GAO Reports in One Step}
\usage{
auto_download(
  format = NULL,
  year = NULL,
  download_dir = "gao_reports",
  sleep_time = 1,
  confirm = TRUE
)
}
\arguments{
\item{format}{Character. \code{"pdf"}, \code{"html"}, or \code{"both"}. \code{NULL} (default)
prompts interactively; in non-interactive sessions defaults to \code{"pdf"}.}

\item{year}{Integer vector of 4-digit fiscal years, e.g. \code{2024} or
\code{2020:2024}. \code{NULL} (default) prompts interactively; in non-interactive
sessions uses all available years.}

\item{download_dir}{Character. Base directory for downloads. \verb{pdf/} and/or
\verb{html/} subdirectories are created beneath it.}

\item{sleep_time}{Numeric. Seconds to pause between downloads.}

\item{confirm}{Logical. If \code{TRUE} (default), prompts for confirmation before
downloading. In non-interactive sessions, \code{confirm = TRUE} raises an error
to prevent accidental mass downloads — set \code{confirm = FALSE} explicitly.}
}
\value{
Invisible character vector of downloaded file paths.
}
\description{
Convenience wrapper that loads the bundled report links, optionally filters
by fiscal year, and downloads reports as PDF, HTML, or both. In interactive
sessions, prompts for format and year range when not supplied.
}
\details{
PDF URLs are constructed directly from report IDs (e.g.,
\verb{/products/gao-24-106198} becomes \verb{/assets/gao-24-106198.pdf}) rather than
scraping each report page, so no extra HTTP requests are needed for
link extraction.
}
\examples{
\dontrun{
# Interactive: walks through prompts
auto_download()

# Non-interactive: download 2024 PDFs
auto_download(format = "pdf", year = 2024, confirm = FALSE)
}
}
